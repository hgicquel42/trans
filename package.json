{
  "name": "trans",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently --kill-others-on-fail 'cd client && npm run dev' 'cd server && npm run start:dev'",
    "build": "concurrently --kill-others-on-fail 'cd client && npm run build' 'cd server && npm run build'",
    "start": "concurrently --kill-others-on-fail 'cd client && npm run start' 'cd server && npm run start'",
    "docker": "docker compose up"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@hapi/joi": "^17.1.1",
    "@types/ws": "^8.5.3",
    "aos": "^2.3.4",
    "concurrently": "^7.1.0",
    "next": "^12.1.5",
    "prisma": "^3.13.0",
    "tailwindcss-animation-delay": "^1.0.7"
  }
}
